## What

<!-- One or two sentences: what skill or change this adds. -->

## Why

<!-- The reason. Link an issue if there is one. -->

## Checklist

- [ ] `python scripts/validate_skills.py` passes (frontmatter `name` + `description`)
- [ ] `python scripts/generate_catalog.py --check` passes (catalog + index in sync)
- [ ] New/changed `SKILL.md` keeps the witness voice and obeys the Blessing Protocol voice register
- [ ] References live under `skills/<name>/references/` and are linked from the SKILL.md
